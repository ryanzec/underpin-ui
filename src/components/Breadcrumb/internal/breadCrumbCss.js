import * as themesCss from 'src/styles/themes';

const theme = themesCss.light;

export const variables = {
  dividerColor: theme.global.gray4,
  dividerPadding: '8px;',
  activeColor: theme.global.gray4,
};
